# censor_values

adds new columns with left and right censoring indicators, setting up
the creation of a Surv object with interval2 format

## Usage

``` r
censor_values(simulated_obs, scale = "log")
```

## Arguments

- scale:
